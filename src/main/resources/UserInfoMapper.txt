// 文件名为当前类的名称

// 当前类所在的包路径
namespace com.demo.mapper

// 导入类 并标志为当前类为实体类
// 导入的实体类只能存在一个
import entity com.demo.data.entity.UserInfo

// 导入其他相关类
import java.util.List
import java.util.ArrayList

search selectById(id: Int, name: String) : UserInfo {
     select * from user_info where id = :id and name = :name
 }

